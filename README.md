## Aplicación para obtener información del clima

Debes instalar los paquetes con el comando: ```npm install```

### Ejemplo para ejecutar

```
    node app -d "New York" -u c
```
> Unidades de temperatura
    c = Centigrados
    f = Farenheit
    k = Kelvin (Por defecto)